# Optional: add a free API key from https://dev.pokemontcg.io/
# Removes rate limits (default: 1000 req/day without a key)
POKEMON_TCG_API_KEY=

# Port for the Express server (default: 3001)
PORT=3001
